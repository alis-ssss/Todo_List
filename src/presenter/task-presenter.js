export default class TaskPresenter {
    constructor(tasksModel) {
        this.tasksModel = tasksModel;
    }

    createTask(title) {
        // Логика для создания задачи
    }

    // Другие методы, связанные с задачами...
}